## Table: canopy_pavan
The `canopy_pavan` table, located in the `dynamic_sld` schema, contains various columns related to tree canopy data. The columns include `gid`, a unique identifier; `point_coun`, `crownareaa`, `treeheight`, `dbh(cm)`, `trunkvolum`, `branchvolu`, `totalvolum`, `trunklengt`, `branchleng`, `branchesnu`, `cbh(m)`, `trunksurfa`, `branchessu`, `totalarea(`, `crownwidth`, `crownareac`, `crownvolum`, `crownare_1`, `crownvol_1`, `classifica`, `confidence`, `centroid`, `wetbiomass`, `drybiomass`, `carbonstoc`, `co2e(kg)`, `carboncred`, `basalarea`, which provide information about tree characteristics and biomass calculations. Additionally, the table includes a geometry column `geom` of type `POLYGON`, representing the spatial boundary of the tree canopy area. This geometry type signifies a two-dimensional shape with a fixed boundary, such as a forest or a wooded area.

## Table: cn_primary_countries
The `cn_primary_countries` table, located in the `dynamic_sld` schema, contains columns related to country information. The columns include `gid`, a unique identifier; `adm0_a3`, `abbrev`, `continent`, `formal_nam`, `iso_a2`, `iso_a3`, `iso_n3`, `iso_short`, `name`, `name_sort`, `postal`, `primary`, `short_name`, `sov_a3`, `sovereign`, `type`, `un_ar`, `un_en`, `un_es`, `un_fr`, `un_n3`, `un_region`, `un_ru`, `un_subregi`, `un_zh`, `wb_a2`, `wb_a3`, `wb_region`, which provide information about country names, codes, and classifications. The table also includes a geometry column `geom` of type `POLYGON`, representing the spatial boundary of a country. This geometry type signifies a two-dimensional shape with a fixed boundary, such as a national border.

## Table: hospitals_in_india
The `hospitals_in_india` table, located in the `dynamic_sld` schema, contains columns related to hospital information in India. The columns include `gid`, a unique identifier; `id`, `city`, `state`, `district`, `density`, `latitude`, `longitude`, `rating`, `number of`, which provide information about hospital locations and characteristics. The table also includes a geometry column `geom` of type `POINT`, representing the spatial location of a hospital. This geometry type signifies a single point in space, such as a hospital building.

## Table: in_district
The `in_district` table, located in the `dynamic_sld` schema, contains columns related to district information. The columns include `gid`, a unique identifier; `dtname`, `stname`, `stcode11`, `dtcode11`, `year_stat`, `dist_lgd`, `state_lgd`, `jid`, which provide information about district names, codes, and statistics. The table also includes a geometry column `geom` of type `POLYGON`, representing the spatial boundary of a district. This geometry type signifies a two-dimensional shape with a fixed boundary, such as a district border.

## Table: india_state_boundary
The `india_state_boundary` table, located in the `dynamic_sld` schema, contains columns related to state boundary information in India. The columns include `gid`, a unique identifier; `state_name`, which provides information about state names. The table also includes a geometry column `geom` of type `POLYGON`, representing the spatial boundary of a state. This geometry type signifies a two-dimensional shape with a fixed boundary, such as a state border.

## Table: road_drrp
The `road_drrp` table, located in the `dynamic_sld` schema, contains columns related to road information. The columns include `gid`, a unique identifier; `er_id`, `state_id`, `block_id`, `district_i`, `drrp_road_`, `roadcatego`, `roadname`, `roadowner`, which provide information about road characteristics and classifications. The table also includes a geometry column `geom` of type `LINESTRING`, representing the spatial path of a road. This geometry type signifies a sequence of connected points, such as a road network.